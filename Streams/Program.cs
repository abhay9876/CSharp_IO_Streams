using System;
using System.IO;
using Streams;
namespace FileHandling
{
    class Program
    {
        public static void Main(string[] args)
        {
            // ReadWrite rw = new ReadWrite();
            // rw.Operations();

            // ReadInput r = new ReadInput();
            // r.Perform();

            // SaveRetrive sr = new SaveRetrive();
            // sr.Operations();

            // ConvertImage ci = new ConvertImage();
            // ci.Operation();

            // ConvertCases cc = new ConvertCases();
            // cc.Perform();

            // PrimitiveData pd = new PrimitiveData();
            // pd.Perform();

            // ThreadCommunication tc = new ThreadCommunication();
            // tc.Operations();

            // LargeFile lf = new LargeFile();
            // lf.ReadPerform();









        }
    }
}